<%= form_for @order, :url => { :action => "update" } do |f| %>

<p> User: 
  <%= @order.user.email %> <br />
  Status:
  <%= f.select :status, Order::STATUS %> <br />
  Description:
  <%= @order.description %> </p>
<div>
  <%= f.submit "Edit" %>
</div>
<% end %>

<%= link_to('Back to orders', admin_orders_path) %>
