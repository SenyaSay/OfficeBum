<%= form_for @characteristic, :url => { :action => action }, :html => { :class => 'form-horizontal' } do |f| %>
  <%= flash[:error] %>

<div class="control-group">
  <%= f.label :name, :class => 'control-label' %>
  <div class="controls">
    <%= f.text_field :name, :class => 'text_field' %>
  </div>
  <%= f.label :level, :class => 'control-label' %>
  <div class="controls">
    <%= f.text_field :level, :class => 'text_field' %>
  </div>
</div>
<div class="characteristic_button">
  <%= f.submit t('button.create'), :class => 'btn btn-primary' %>
  <%= link_to t('button.back'), admin_characteristics_path, :class => 'btn' %>
</div>
<% end %>
