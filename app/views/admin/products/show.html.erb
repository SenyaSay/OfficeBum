<%- model_class = Product -%>
<div class="page-header">
  <h3><%= t("titles.#{controller.controller_name}.#{controller.action_name}") %></h3>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= t('.name') %>:</strong></dt>
  <dd><%= @product.name %></dd>
  <dt><strong><%= t('.category') %>:</strong></dt>
  <dd><%= @product.category.try(:name) %></dd>
  <dt><strong><%= t('.price') %>:</strong></dt>
  <dd><%= @product.price %></dd>
  <dt><strong><%= t('.description') %>:</strong></dt>
  <dd><%= @product.description %></dd>
  <dt><strong><%= t('.image') %>:</strong></dt>
  <dd><%= image_tag(@product.image_url(:thumb)) if @product.image? %></dd>
</dl>

<div class="form-actions">
  <%= link_to t('button.back'), admin_products_path, :class => 'btn'  %>
  <%= link_to t('button.edit'), edit_admin_product_path(@product), :class => 'btn' %>
  <%= link_to t('button.delete'),
              admin_product_path(@product),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
</div>
